import { formatBRL } from '@/lib/format/currency'
import { MetricCard } from '@/components/ui/metric-card'

export function SalesTargetCard({ target, realizedSales, targetGap, achievementPercent, billedThrough }: { target: number; realizedSales: number; targetGap: number; achievementPercent: number | null; billedThrough: string | null }) {
  return <section aria-labelledby="sales-target-title" className="space-y-3"><h2 id="sales-target-title" className="text-lg font-semibold">Meta do mês</h2><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><MetricCard label="Meta do mês" value={formatBRL(target)} accentColor="navy" /><MetricCard label="Faturamento realizado" value={formatBRL(realizedSales)} accentColor="green" footnote="Fonte: faturamento Tiny/Olist" /><MetricCard label="Falta para meta" value={formatBRL(Math.max(targetGap, 0))} accentColor={targetGap >= 0 ? 'green' : 'red'} /><MetricCard label="Atingimento" value={achievementPercent == null ? 'Sem meta' : `${achievementPercent.toFixed(1)}%`} accentColor={achievementPercent != null && achievementPercent >= 100 ? 'green' : 'brown'} footnote={billedThrough ? `Dados faturados disponíveis até ${new Date(`${billedThrough}T00:00:00Z`).toLocaleDateString('pt-BR')}` : 'Sem faturamento com data disponível para o mês'} /></div></section>
  }
